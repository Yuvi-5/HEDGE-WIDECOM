[pytest]
testpaths = tests
pythonpath = src
addopts = --tb=short -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning:edge_sim_py
    ignore::RuntimeWarning:scipy
markers =
    integration: marks tests as end-to-end integration tests (run with actual simulations)
